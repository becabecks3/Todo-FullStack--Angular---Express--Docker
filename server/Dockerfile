FROM node:latest
WORKDIR /server
COPY package*.json ./
RUN npm i 

ARG BACKEND_LISTEN_PORT
ARG MG_CONECTION


COPY . .

ENV  BACKEND_KEY=${BACKEND_LISTEN_PORT}
ENV  MG_KEY=${MG_CONECTION} 

CMD ["npm", "start"]
EXPOSE 3000
